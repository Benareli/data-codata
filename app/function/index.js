const id = require("./id.function");
const coa = require("./coa.function");
const cache = require("./cache.function");
const journal = require("./journal.function");
const stock = require("./stock.function");

module.exports = {
  id,
  coa,
  cache,
  journal,
  stock
};